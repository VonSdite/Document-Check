# 项目协作要求

1. 使用 git 管理每次修改，每次完成一组有意义的修改后提交，commit message 使用中文。
2. 设置并维护合理的 `.gitignore`，避免提交本地数据库、上传文件、密钥、虚拟环境和缓存文件。
3. 所有源代码、模板、样式、配置样例和文档统一使用 UTF-8 编码。
4. 所有文本文件统一使用 LF 换行符。
5. 管理员账号、密码、隐藏管理入口、监听地址和启动端口配置在本地 `config.yaml` 中，真实配置不提交到仓库。
6. SQLite 数据库与上传文件保存在本地 `instance/` 目录中，作为运行时数据处理。
7. 使用 uv 管理 Python 依赖和虚拟环境，依赖声明维护在 `pyproject.toml`，锁定文件为 `uv.lock`，启动使用 `uv run python run.py`。
8. 项目注释、README 和其他说明文档使用正面陈述，保持简洁直接，描述当前行为、设计目标、约束和使用方法；不写历史原因、迁移背景、已删除实现或其他非当前行为的信息。
9. 每次提交前使用 Ruff 格式化所有 Python 源代码，并执行 Ruff 检查。

## 文档索引

- [README.md](README.md)：项目功能、配置、运行和使用说明。
- [4+1 架构视图](docs/architecture-4plus1.md)：系统的场景、逻辑、开发、进程和物理视图。

- [开发与模块边界](docs/development.md)：模块职责、依赖方向、扩展方式和回归验证。
- [性能与容量验证](docs/performance.md)：数据库查询策略、文档处理与并发压测。
